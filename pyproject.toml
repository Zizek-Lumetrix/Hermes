[project]
name = "hermes"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "feedparser>=6.0",
    "html2text>=2024.2",
    "openai>=1.0",
    "sentence-transformers>=3.0",
    "python-frontmatter>=1.0",
    "pyyaml>=6.0",
]

[project.scripts]
hermes = "hermes.__main__:main"
